[package]
name = "hd60s-api"
version = "0.1.0"
edition = "2024"
description = "The state and the client of the hd60s-linux service, shared by the service, its command line and the control program"
license = "MIT"
repository = "https://github.com/Hydrogen2K/hd60s-linux"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
